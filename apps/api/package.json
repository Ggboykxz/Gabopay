{
  "name": "api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "uvicorn apps.api.main:app --reload --host 0.0.0.0 --port 8000",
    "build": "echo 'No build step for API'",
    "start": "uvicorn apps.api.main:app --host 0.0.0.0 --port 8000",
    "lint": "ruff check apps/api",
    "test": "pytest apps/api/tests"
  }
}